<form method="post" action="{{ path('app_annotation_delete', {'id': annotation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annotation.id) }}">
    <button class="btn">Delete</button>
</form>
